# ADR 004: PostgreSQL 采用先写入后切读取的演进策略

## 状态

已接受

## 决策

先实现将内存快照持久化到 PostgreSQL，再逐步把查询路径迁移到数据库。

## 原因

- 不会破坏现有动态原型
- 能先验证表结构与持久化流程
- 后续可逐步切换 `overview / explorer / search / graph` 的读路径

## 影响

- 短期内存在两套读取路径
- 需要在文档里明确当前数据库仅承担持久化职责
